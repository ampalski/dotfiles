# BACKGROUND
background {
    monitor =
    # path = screenshot
    path=/home/andrew/dotfiles/wallpapers/Pictures/Wallpapers/astronaut_converted.png
    color = rgba(25, 20, 20, 1.0)
    blur_passes = 1
}

# GENERAL
general {
    no_fade_in = true
    no_fade_out = true
    hide_cursor = false
    grace = 0
    disable_loading_bar = true
}

# INPUT FIELD
# input-field {
#     monitor =
#     size = 250, 60
#     outline_thickness = 2
#     dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
#     dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
#     dots_center = true
#     outer_color = rgba(0, 0, 0, 0)
#     inner_color = rgba(0, 0, 0, 0.2)
#     font_color = $foreground
#     fade_on_empty = false
#     rounding = -1
#     check_color = rgb(204, 136, 34)
#     placeholder_text = <i><span foreground="##cdd6f4">Input Password...</span></i>
#     hide_input = false
#     position = 0, -200
#     halign = center
#     valign = center
# }
input-field {
    monitor =
    size = 300, 40
    outline_thickness = 3
    inner_color = rgba(1f2335ee) # no fill

    outer_color = rgba(a9b1d6ee) rgba(4fd6be00) 45deg
    check_color=rgba(00ff99ee) rgba(ff6633ee) 120deg
    fail_color=rgba(ff6633ee) rgba(ff0066ee) 40deg

    font_color = rgb(ff757f)
    fade_on_empty = false
    rounding = 15

    position = 0, -20
    halign = center
    valign = center
}

# DATE
label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%A, %B %d")"
  color = rgba(c3e88daa)
  font_size = 22
  font_family = SpaceMono Nerd Font
  position = 0, 300
  halign = center
  valign = center
}

# TIME
label {
  monitor = 
  text = cmd[update:1000] echo "$(date +"%-I:%M")"
  color = rgba(7dcfffaa)
  font_size = 95
  font_family = SpaceMono Nerd Font
  position = 0, 200
  halign = center
  valign = center
}

# Profile Picture
# image {
#     monitor =
#     path = /home/justin/Pictures/profile_pictures/justin_square.png
#     size = 100
#     border_size = 2
#     border_color = $foreground
#     position = 0, -100
#     halign = center
#     valign = center
# }

# Desktop Environment
# image {
#     monitor =
#     path = /home/andrew/Pictures/Wallpapers/samurai_dark_original.jpg
#     size = 75
#     border_size = 2
#     border_color = $foreground
#     position = -50, 50
#     halign = right
#     valign = bottom
# }

# label {
#     monitor =
#     text = cmd[update:1000] echo "$(/home/justin/Documents/Scripts/whoami.sh)"
#     color = $foreground
#     font_size = 14
#     font_family = JetBrains Mono
#     position = 0, -10
#     halign = center
#     valign = top
# }


